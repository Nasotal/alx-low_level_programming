#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/*
 * a function-like macro ABS(y)
 * that computes the absolute value of a number y
 */

#define ABS(y) ((y) < (0) ? -(y) : (y))

#endif /* _FUNCTION_LIKE_MACRO_H */
